const restaurants = require('./restaurants')
const users = require('./users')
const sessions = require('./sessions')
const restaurantRecipes = require('./restaurants/recipes')

module.exports = {
  restaurants,
  users,
  sessions,
  restaurantRecipes,
}
